#include <vector>
#include <iostream>
#include <utility>
#include <stdlib.h>

using namespace std;
//retorna o custo total como descrito no enunciado
int solution(const vector<int> & input, int L) {
  return -1;
}
